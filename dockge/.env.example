# dockge — copy to /srv/homelab/secrets/dockge.env on the Pi (RB-002).
# Dockge's own admin login is created on first visit and stored in
# /app/data (backed up); the UI additionally sits behind Authelia
# two_factor.
TZ=Europe/Berlin
