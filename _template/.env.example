# myservice — copy to /srv/homelab/secrets/myservice.env on the Pi and
# fill in real values (RB-002). Even secretless stacks need this file:
# deploy.sh requires it (TZ at minimum).
TZ=Europe/Berlin
